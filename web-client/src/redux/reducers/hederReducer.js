let initialState = {
    headerMessage: 'Header. Hello Username!'
}

const headerReducer = (state = initialState, action) => {
    switch (action.type){
        default:
            return state
    }
}

export default headerReducer
