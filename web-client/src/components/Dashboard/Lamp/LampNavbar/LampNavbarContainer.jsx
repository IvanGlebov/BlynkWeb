import LampNavbar from "./LampNavbar";
import {connect} from "react-redux";

const mapStateToProps = (state) => {
    return{

    }
}

const mapDispatchToProps = (dispatch) => {
    return {

    }
}

const LampNavbarContainer = connect(mapStateToProps, mapDispatchToProps)(LampNavbar)

export default LampNavbarContainer